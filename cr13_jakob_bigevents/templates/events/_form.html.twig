{{ form_start(form) }}
	<div class="d-flex justify-content-center">
	    {{ form_widget(form, {'attr': {'class': 'form'}}) }}
	</div>

    <div class="d-flex justify-content-center">
	    <button class="btn btn-success">{{ button_label|default('Save') }}</button>

	    <a href="{{ path('events_index') }}" class='btn btn-info'>back to list</a>
	</div>
{{ form_end(form) }}